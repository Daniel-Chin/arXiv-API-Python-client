[project]
name = "arxiv-api-python-client"
version = "0.1.0"
description = "A python client over export.arxiv.org"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "platformdirs>=4.3.7",
]
